<div class="body_column">
  <h1>Matt's Blog Users</h1>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to "#{user.username}", user_path(user) %> (<%= user.email %>)</li>
    <% end %>
  </ul>
</div>
